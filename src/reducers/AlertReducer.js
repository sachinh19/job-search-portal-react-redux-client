import * as constants from '../constants'

const AlertReducer = (state = null, action) => {

    switch (action.type) {

        default :
            return state
    }
};

export default AlertReducer;